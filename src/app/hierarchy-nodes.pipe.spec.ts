import { HierarchyNodesPipe } from './hierarchy-nodes.pipe';

describe('HierarchyNodesPipe', () => {
  it('create an instance', () => {
    const pipe = new HierarchyNodesPipe();
    expect(pipe).toBeTruthy();
  });
});
